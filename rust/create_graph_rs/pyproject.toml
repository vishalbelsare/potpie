[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "create_graph_rs"
version = "0.1.0"
requires-python = ">=3.10"

[tool.maturin]
module-name = "create_graph_rs"
manifest-path = "Cargo.toml"

[tool.uv]
package = true
